# 生产环境 API 地址示例
# 复制此文件为 .env.production:
#   cp .env.production.example .env.production
#
# 同源 nginx 反代 /api → backend（默认，无需修改）
VITE_API_BASE_URL=/api
#
# 若前后端分离部署（不同域名），改为完整 URL:
# VITE_API_BASE_URL=https://your-backend-domain.com/api
